"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
//  weak

var requirePropFalseFactory = function requirePropFalseFactory(componentNameInError) {
  var requirePropFalse = function requirePropFalse(requiredFalseProp) {
    return function (props, propName, componentName, location, propFullName) {
      var propFullNameSafe = propFullName || propName;
      var requiredFalsePropArray = Array.isArray(requiredFalseProp) ? requiredFalseProp : [requiredFalseProp];

      for (var i = 0; i < requiredFalsePropArray.length; i += 1) {
        var testProp = requiredFalsePropArray[i];

        if (props[propName] && props[testProp]) {
          return new Error("The property `" + propFullNameSafe + "` of " + ("`" + componentNameInError + "` should not be `true` at the ") + ("same time as `" + testProp + "` is `true`."));
        }
      }

      return null;
    };
  };

  return requirePropFalse;
};

exports.default = requirePropFalseFactory;